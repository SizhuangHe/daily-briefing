"""
News aggregation service.

Handles RSS fetching, article deduplication (triple strategy),
etag caching, and article storage.
"""


# TODO: Implement in Phase 3
# - fetch_rss_feed(url) -> list[Article]
# - fetch_all_sources() -> list[Article]
# - store_articles(articles) with dedup
# - extract_topics(article) -> list[str]
